@model KnowledgeBase.Logic.Dto.Project.ProjectDto;
@{
}
@using (Html.BeginForm("FoundProject", "Project", FormMethod.Post))
{
@Html.AntiForgeryToken()
<div>
        Enter name, description on Onetag name<br />
        @Html.EditorFor(Model => Model.Name)
        @Html.ValidationMessageFor(Model => Model.Name)

        <br />
        <br />
        Start Date:<br />
        <input class="form-control" asp-for="DateFrom" asp-format="{0:yyyy-MM-dd}" />
        <br />
        <br />

        End Date:
        <br />
        <input class="form-control" asp-for="DateTo" />
        <br />
        <br />

        Enter the name of the tag, separating it with a space<br />
        @Html.EditorFor(Model => Model.Tag.Name)
        @Html.ValidationMessageFor(Model => Model.Tag.Name)<br /><br /><br />
        <a asp-route="FoundProject"><input type="submit" value="Find Projects" /></a>
    </div>
}
