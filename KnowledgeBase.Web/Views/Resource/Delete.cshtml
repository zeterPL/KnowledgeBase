@model KnowledgeBase.Logic.Dto.ResourceDto
@{
	ViewData["Title"] = "SoftDelete";
}

<h2>creator</h2>

@using (Html.BeginForm("SoftDelete", "Resource", FormMethod.Post))
{
	@Html.AntiForgeryToken()
	<div>
		@Model.Name || @Model.Description || @Model.Category
	</div>
	<div>
		<input type="submit" value="Submit" />
		<input type="submit" asp-action="Index"  value="Discard" />
	</div>
}